---
label: test
hide_body: true
fields:
- name: sadfsdf
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: sadfsdf
- name: res
  type: field_group_list
  fields:
  - name: tste
    type: text
    config:
      required: false
    label: tste
  - name: asdfs
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: asdfs
  config:
    min: 
    max: 
    labelField: 
  label: res
- name: asss
  type: blocks
  label: asss
  template_types:
  - project
  config:
    min: 
    max: 
