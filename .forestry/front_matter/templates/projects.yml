---
label: Projects
hide_body: true
fields:
- name: projects
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
    description: give that project an epic name dude
  - name: description
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Description
    description: Give a few words for your project
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    description: A juicy preview of your creation
  config:
    min: 
    max: 
    labelField: 
  label: Projects
  description: Orderable list of projects, in a UI. :))) epic
